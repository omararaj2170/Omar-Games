const words=['apple','grape','brave','flame','sound'];let secret=words[Math.floor(Math.random()*words.length)];game.innerHTML='<input id=g maxlength=5><button id=s>Guess</button><div id=o></div>';s.onclick=()=>{const v=g.value.toLowerCase();if(v.length<5)return;let res='';for(let i=0;i<5;i++)res+=v[i]==secret[i]?'🟩':secret.includes(v[i])?'🟨':'⬛';o.innerHTML+=`<p>${v}: ${res}</p>`;if(v==secret)o.innerHTML+='<p>You win!</p>';g.value='';};score.textContent='Guess the 5-letter word';