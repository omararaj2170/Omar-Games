const words=['brave','stone','light'];let w=words[Math.floor(Math.random()*words.length)],used=[],left=6;game.innerHTML='<input id=l maxlength=1><button id=a>Try</button><p id=mask></p><p id=used></p>';const draw=()=>{mask.textContent=w.split('').map(ch=>used.includes(ch)?ch:'_').join(' ');usedEl.textContent='';};const usedEl=document.getElementById('used');function render(){mask.textContent=w.split('').map(ch=>used.includes(ch)?ch:'_').join(' ');usedEl.textContent='Used: '+used.join(',');score.textContent='Lives: '+left;if(!mask.textContent.includes('_'))score.textContent='You win!';if(left<=0)score.textContent='You lose: '+w;}a.onclick=()=>{const ch=l.value.toLowerCase();if(!ch||used.includes(ch))return;used.push(ch);if(!w.includes(ch))left--;render();l.value='';};render();