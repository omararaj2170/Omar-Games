let n=1+Math.floor(Math.random()*40),tries=0;game.innerHTML='<input id=i type=number min=1 max=40><button id=g>Guess</button>';const iEl=document.getElementById('i');document.getElementById('g').onclick=()=>{tries++;const v=+iEl.value;score.textContent=v===n?`Correct in ${tries} tries`:v<n?'Too low':'Too high';};reset.onclick=()=>{n=1+Math.floor(Math.random()*40);tries=0;score.textContent='New number ready';};